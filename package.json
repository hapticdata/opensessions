{
  "name": "opensessions",
  "version": "0.2.0-alpha.5",
  "private": true,
  "packageManager": "bun@1.3.5",
  "bin": {
    "opensessions-sidebar": "bin/opensessions-sidebar"
  },
  "workspaces": ["apps/*", "packages/*", "packages/mux/*", "packages/mux/providers/*"],
  "scripts": {
    "build": "turbo build",
    "test": "turbo test",
    "postinstall": "node scripts/postinstall.js",
    "dev": "turbo dev",
    "start:server": "bun run apps/server/src/main.ts",
    "start:tui": "cd apps/tui && bun run start",
    "dev:server": "cd apps/server && bun run dev",
    "dev:tui": "cd apps/tui && bun run dev"
  },
  "devDependencies": {
    "turbo": "^2",
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
